// Generated by CoffeeScript 1.9.3
(function() {
  var _, check, flag, flagger, matchKey;

  _ = require('underscore');

  flagger = require('flagger')();

  flag = flagger.print || flagger.p;

  matchKey = function(keys) {
    if (typeof keys === 'string') {
      return _.contains(flag, keys);
    }
    return _.any(keys, function(key) {
      return _.contains(flag, key);
    });
  };

  check = function(keyString) {
    return flag && (flag.length === 0 || matchKey(keyString));
  };

  module.exports = {
    log: function(keyString) {
      var logArguments;
      if (check(keyString)) {
        logArguments = Array.prototype.slice.call(arguments, 1);
        return console.log.apply(console, logArguments);
      }
    }
  };

}).call(this);
